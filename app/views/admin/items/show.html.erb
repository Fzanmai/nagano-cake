<div class="container px-5 px-sm-0 mt-5">
<div class="row">
   <h2>商品詳細</h2>

 <div class = "row my-5 d-flex justify-content-center">
   <p><%= image_tag @item.get_item_image(300,300) %></p>

   <table>
   	<tr><th>商品名</th><td><%= @item.name %></td></tr>

    <tr><th>商品説明</th><td><%= @item.introduction %></td></tr>

    <tr><th>ジャンル</th><td><%= @item.item_genre.name %></td></tr>

    <tr><th>税抜価格(税込価格）</th><td><%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</td></tr>

    <tr><th>販売ステータス</th><td><% if @item.is_active? %>
　　<span class="text-active text-success">販売中</span>
　　<% else %>
　　<tr><th><span class="text-invalid text-secondary">販売停止中</span></td></tr>
　　<% end %></td></tr>

    <tr><th><%= link_to "Edit", edit_admin_item_path(@item.id), class: "btn btn-success" %></td></tr>
    <tr><th><%= link_to "back", admin_items_path %></td></tr>

   </table>
   </div>
   </div>
   </div>
   </div>