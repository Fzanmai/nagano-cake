<h2>マイページ</h2>
  <h4>登録情報</h4><%= link_to '編集する', edit_customer_path(@customer), class: "btn btn-sm btn-success" %>
  <div>
    氏名<%= @customer.last_name %><%= @customer.first_name %>
  </div>
  <div>
    カナ<%= @customer.last_name_kana %><%= @customer.first_name_kana %>
  </div>
  <div>
    郵便番号<%= @customer.post_code %>
  </div>
  <div>
    住所<%= @customer.address %>
  </div>
  <div>
    電話番号<%= @customer.phone_number %>
  </div>
  <div>
    メールアドレス<%= @customer.email %>
  </div>


<h4>配送先</h4>
<h4>注文履歴</h4>